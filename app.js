javascript
// 输入 con 就会自动提示 console.log
console.log('Hello from VSCode!');

// 定义一个函数
function greet(name) {
    return `Hello, ${name}!`;
}

// 调用函数
const message = greet('GitHub');
console.log(message);

// 测试循环
for (let i = 0; i < 5; i++) {
    console.log(`Count: ${i}`);
}